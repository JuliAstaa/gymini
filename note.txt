Nama anggota
    1. I Made Juli Astawa       2401010239
    2. I Nengah Saitya Sadipta  2401010234
    3. I Made Indrawan          2401010237

Penjelasan aplikasi
GYMINI adalah sebuah aplikasi yang kami buat, aplikasi ini bertipe manajemen karena mengelola aplikasi ini mengelola gym. Aplikasi ini memiliki database untuk menyimpan data data, seperti data member, user, membership pla, dan yang lainnya. Adapun collation pada table yang kami gunakan yaitu utf8mb4_0900_ai_ci, karena ini adalah default collation pada mysql versi 8.0.30. Untuk melakukan CRUD (Create, Read/Retrieve, Update, Delete) kami menggunakan menggunakan RESTful API yang dimana menggunakan HTTP request untuk mengirim dan mengelola data, untuk membuat API nya kami menggunakan bahasa pemrograman PHP. Untuk tampilan frontend atau dari sisi client itu sudah dinamis dengan menggunakan jQuery

Pembagian tugas:
1. I Made Juli Astawa 
    Tugas saya disini adalah mengelola backend, mulai dari membuat models untuk menyimpan query untuk memasukkan data ke database, validator untuk mengecek apakah data yang dikirim oleh client dari sisi client-side itu udah valid apa belum, dan yang terakhir itu membuat API nya. Cara kerjanya yaitu, data yang dikirim oleh endpoint API nya akan masuk ke endpoint tersebut dengan method yang sesuai, misal create itu pakai POST, read pakai GET update pakai PATCH / PUT, dan delete pakai DELETE. Kemudian data akan diambil dari body yang dikirim dari endpoint tersebut dengan format JSON, setelah itu akan dilakukan pengecekan apakah json itu valid atau tidak, apakah data itu sudah bersih apa tidak, setelah di validasi data tersebut akan di sanitasi. Jika semua itu sudah selesai maka data tersebut akan dikirim ke models melalui fungsi yang sudah disediakan. Jika berhasil akan mengembalikan response, jika gagal juga akan mengembalikan response